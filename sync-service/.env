# ─────────────────────────────────────────────────
#  YojanaMitra Sync Service — Environment Variables
#  Copy this file to `.env` and fill in the values.
# ─────────────────────────────────────────────────

# MongoDB connection string (required)
# Format: mongodb://user:password@host:port  OR  mongodb+srv://...
MONGO_URI=mongodb+srv://YojnaMitra:YojnaMitra@cluster0.0z8waq6.mongodb.net/YojnaMitra?retryWrites=true&w=majority&appName=Cluster0

# MyScheme API key (required) — obtained from api.myscheme.gov.in
API_KEY=tYTy5eEhlu9rFjyxuCr7ra7ACp4dv1RH8gWuHTDc

# Application environment: development | production
NODE_ENV=development

# Logging level: error | warn | info | debug  (default: info)
LOG_LEVEL=info

# Set to "true" to trigger an immediate sync when the scheduler starts
SYNC_ON_START=false

# Exact schemeCategory value used by the MyScheme API filter.
# Must match the API's category identifiers exactly (case-sensitive).
# Common values:
#   Agriculture,Rural & Environment
#   Health & Wellness
#   Education & Learning
#   Social welfare & Empowerment
# Leave empty to sync ALL categories.
SYNC_CATEGORY=Agriculture,Rural & Environment

# Set to "true" (CLI only) to run in test mode — processes only 5 schemes
TEST_MODE=true

# Set to "true" (CLI only) to run in dry-run mode — no DB writes
DRY_RUN=false
