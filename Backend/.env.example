# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb+srv://YojnaMitra:YojnaMitra@cluster0.0z8waq6.mongodb.net/?YojnaMitra=Cluster0

# Schemes database — same value as sync-service MONGO_URI
# The sync-service stores scheme documents here; Backend reads them for matching
SCHEMES_MONGO_URI=mongodb://localhost:27017/yojanamitra

# Gemini API — for parseEligibilityLLM.js (scheme enrichment, run once)
# Get free key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash


# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Email Configuration (FREE - Recommended)
# Use Gmail, Outlook, or any SMTP service
EMAIL_SERVICE=gmail
EMAIL_USER=loyamahesh11@gmail.com
EMAIL_PASSWORD=tzveqmnjvdsktmje

# OTP Service (Twilio) - REQUIRES PAYMENT
OTP_SERVICE=twilio
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# WhatsApp (Twilio) - your Twilio WhatsApp sender number (without whatsapp: prefix)
# Sandbox default: +14155238886  |  Production: your approved number
TWILIO_WHATSAPP_NUMBER=+14155238886

# Secure link JWT (separate from auth JWT — used for WhatsApp dashboard links)
LINK_JWT_SECRET=your-link-jwt-secret-change-in-production
LINK_JWT_EXPIRE=24h

# Frontend URL (used to generate dashboard links sent via WhatsApp)
FRONTEND_URL=http://localhost:5173

# Alternative: MSG91 (Recommended for India) - REQUIRES PAYMENT
# Get credentials from https://msg91.com/
# For development: Leave empty (OTP logs to console)
# For production: Set NODE_ENV=production and add credentials
MSG91_AUTH_KEY=your-msg91-auth-key
MSG91_SENDER_ID=YOJMTR

# WhatsApp Chatbot Configuration (Twilio WhatsApp Sandbox - FREE for testing)
# Get credentials from https://console.twilio.com/
# Use WhatsApp Sandbox for development (no Business account needed)
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886
MSG91_TEMPLATE_ID=your-dlt-template-id

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
